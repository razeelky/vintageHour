import React from 'react'

function Support() {
  return (
    <div className='container py-5'>
      <h2>Support & Info</h2>
      <p>All watches are vintage pre-owned pieces that are inspected before listing.</p>
      <p>Each user cart is stored in the backend and converted to an order during checkout.</p>
      <p>Admin inventory actions are protected and available only after admin login.</p>
    </div>
  )
}

export default Support
